<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
    <channel>
        <title>FoxSay Updates</title>
        <link>https://updates.skulkworks.dev/foxsay/appcast.xml</link>
        <description>Updates for FoxSay</description>
        <language>en</language>
        <item>
            <title>Version 2.1.1</title>
            <pubDate>Wed, 26 Aug 2026 20:01:55 +0100</pubDate>
            <sparkle:version>13</sparkle:version>
            <sparkle:shortVersionString>2.1.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>New</h3>
<ul>
<li>Stop button on a model download, for a download that is going nowhere or a model you picked by mistake. What has already been fetched is kept, so starting again carries on from there rather than going back to the beginning</li>
<li>Unused model files row in Speech Models, offering to remove the copy an earlier version of FoxSay downloaded into a folder this version no longer reads. It shows how much that copy is taking up, and only ever removes that older copy</li>
</ul>
<h3>Improved</h3>
<ul>
<li>A model download says "Preparing…" while it works out what to fetch, instead of sitting at 0% for the several seconds that takes</li>
<li>Debug builds are now a separate app, <code>FoxSay (DEV)</code>, with their own bundle identifier. macOS ties privacy permissions to the identifier and code signature, so a debug build sharing the released app's identifier had Microphone and Accessibility revoked on every rebuild, and granting them took them away from the released copy. Each now has its own entries, granted once. Models, history and statistics stay shared; settings do not. Automatic update checks are off in debug builds, so the dev build cannot install the released version over itself</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Download progress reaching 80% within seconds and then sitting there for the rest of the download, however long that took. Speech models and AI models both report the real fraction fetched now, straight from the download, so the bar tracks the bytes arriving and reaches 100% when the model is ready</li>
<li>Updating from FoxSay 1.x reporting an already-downloaded speech model as missing and fetching the whole thing again. The folder these models are kept in was renamed between the two versions, so the model was still on the disk, just not where FoxSay had started looking. A Parakeet V2 model downloaded by 1.x is now picked up where it lies. Parakeet V3's files changed at the same time and cannot be reused, so that one still needs downloading again</li>
<li>Accuracy card on the dashboard sitting lower than the other three statistics, because it had no trend line to fill the row</li>
<li>Status in the sidebar footer not being clickable — it reports permission and model problems, so it now jumps to System Status on the dashboard, where those are fixed</li>
</ul>
            ]]></description>
            <enclosure
                url="https://updates.skulkworks.dev/foxsay/FoxSay-2.1.1-13.dmg"
                length="21262013"
                type="application/octet-stream"
                sparkle:edSignature="yaHNdVyGGzzvr157QEiN6HaIwptP+jSHiGlJ7sbfBbIJi213svVx3naZf2evhDaS4l54uQDrlgiYYVZ11m/lCw=="
            />
        </item>
    </channel>
</rss>
